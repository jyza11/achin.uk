PUBLIC_PB_URL=http://127.0.0.1:8090

# Set to 1 to let `npm run build` prerender from the bundled fallback content when PocketBase
# is unreachable or empty, instead of failing the build loudly. Leave unset for a real deploy.
# ALLOW_FALLBACK_BUILD=
