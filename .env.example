PUBLIC_PB_URL=http://127.0.0.1:8090

# Set to 1 in production once the API is public, so `npm run build` fails loudly instead of
# silently prerendering from a stale committed content snapshot when PocketBase is unreachable
# or empty. Leave unset until then.
# REQUIRE_CMS_BUILD=
